    static int niceArray(int[] a) {
        int value = 0;
        for (int i = 0; i <= a.length - 1; i++) {
            if (isPrime(a[i]) && a[i] > 0)
                value += a[i];
        }
        return value == a[0] ? 1 : 0;
    }
