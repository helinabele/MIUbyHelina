    static int isMeera(int[] a) {
        int sum = 0;
        for (int i = 0; i < a.length; i++) {
            if (a[i] < i)
                sum += a[i];
        }
        return sum == 0 ? 1 : 0;
    }